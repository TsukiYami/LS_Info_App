services:
  backendinfoapp:
    image: ${DOCKER_REGISTRY-}backendinfoapp
    build:
      context: .
      dockerfile: BackendInfoApp/Dockerfile
    ports:
      - "8080:8080"
  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_USER: ADMIN
      POSTGRES_PASSWORD: ADMIN
    ports:
      - "5432:5432"